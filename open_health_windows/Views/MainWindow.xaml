<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="open_health_windows.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:open_health_windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Open Health">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />

        </Grid.RowDefinitions>
        <TitleBar x:Name="AppTitleBar"
      Title="Open Health">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="ms-appx:///Assets/app_logo.png"/>
            </TitleBar.IconSource>
        </TitleBar>

        <InfoBar x:Name="AppInfoBar" IsOpen="False" />

        <Grid Grid.Row="1" RowSpacing="20" Margin="20,20,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Content="Load new image" Grid.Column="0" Grid.Row="0"/>
            <Button Content="Analyze Image" Grid.Column="0" Grid.Row="1"/>
            <Button Content="Save Result" Grid.Column="0" Grid.Row="2"/>
            <DropDownButton Grid.Column="0" Grid.Row="3">
                <TextBlock FontFamily="Segoe" FontSize="14" Text="Choose hardware"/>
                <DropDownButton.Flyout>
                    <MenuFlyout Placement="BottomEdgeAlignedLeft">
                        <MenuFlyoutItem Text="CPU" Icon="AlignLeft" Tag="left"/>
                        <MenuFlyoutItem Text="NPU" Icon="AlignCenter" Tag="center"/>
                    </MenuFlyout>
                </DropDownButton.Flyout>
            </DropDownButton>
            <Image Source="/Assets/cat.jpg" Grid.Column="1" MaxWidth="200" Margin="100,0,100,0" Grid.RowSpan="4"/>
        </Grid>
        <HyperlinkButton Content="View on GitHub" VerticalAlignment="Bottom" Grid.Row="2" HorizontalAlignment="Right" Click="GitHubButton_Click" Margin="20,20,20,20"/>
    </Grid>
</Window>
